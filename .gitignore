# Local Claude Code MCP config — copy from .mcp.json.example and fill in
# your own tunnel hostname. Kept out of git so a placeholder URL never
# auto-loads (and fails) in someone else's session.
.mcp.json
